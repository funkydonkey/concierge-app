# Voice Notes Service — Техническое задание

## 📋 Общая информация

| Параметр | Значение |
|----------|----------|
| **Название проекта** | Voice Notes Service |
| **Тип** | Персональный сервис для обработки голосовых заметок |
| **Автор** | Andrey |
| **Дата создания** | 2025-01-17 |
| **Статус** | В разработке |

---

## 🎯 Цель проекта

Создать сервис, который позволяет:
1. Записывать голосовые заметки с телефона (iOS Shortcuts)
2. Автоматически транскрибировать речь в текст (OpenAI Whisper)
3. Анализировать контент с помощью AI и выполнять действия:
   - Создавать новые заметки в Obsidian (через GitHub)
   - Добавлять задачи в единый TODO-лист в Obsidian
   - Дополнять существующие заметки

---

## 🏗️ Архитектура

```
┌─────────────────┐     HTTP POST      ┌─────────────────────────────────┐
│  📱 iPhone      │ ─────────────────▶ │     🖥️ FastAPI Backend          │
│  iOS Shortcuts  │     /api/voice     │     (Render.com)                │
└─────────────────┘                    │                                 │
                                       │  ┌───────────────────────────┐  │
                                       │  │ 1. Whisper API            │  │
                                       │  │    (транскрипция)         │  │
                                       │  └─────────────┬─────────────┘  │
                                       │                │                │
                                       │  ┌─────────────▼─────────────┐  │
                                       │  │ 2. AI Agent               │  │
                                       │  │    (OpenAI Agents SDK)    │  │
                                       │  │    - анализ контента      │  │
                                       │  │    - выбор действий       │  │
                                       │  └─────────────┬─────────────┘  │
                                       │                │                │
                                       │  ┌─────────────▼─────────────┐  │
                                       │  │ 3. GitHub API             │  │
                                       │  │    - создание файлов      │  │
                                       │  │    - обновление TODO.md   │  │
                                       │  └───────────────────────────┘  │
                                       └───────────────┬─────────────────┘
                                                       │
                                                       ▼ Git commit + push
                                       ┌─────────────────────────────────┐
                                       │  📦 GitHub Repository           │
                                       │  (Obsidian vault - приватный)   │
                                       └───────────────┬─────────────────┘
                                                       │
                                                       ▼ Auto-pull (каждые 5 мин)
                                       ┌─────────────────────────────────┐
                                       │  💻 Obsidian + Obsidian Git     │
                                       │  (на твоём компьютере)          │
                                       └─────────────────────────────────┘
```

### Почему GitHub вместо прямого доступа к Obsidian?

Obsidian работает локально на компьютере, а сервис — на Render.com в облаке. 
Они не могут общаться напрямую. GitHub выступает посредником:

1. **Сервис** создаёт/обновляет файлы через GitHub API
2. **GitHub** хранит все заметки в репозитории
3. **Obsidian Git плагин** автоматически подтягивает изменения

**Бонусы:**
- Работает даже когда компьютер выключен
- Автоматический бэкап всех заметок
- История изменений через Git

### ⚠️ Важные ограничения

| Ограничение | Описание | Решение |
|-------------|----------|---------|
| **Задержка синхронизации** | Заметки появятся в Obsidian через 1-5 минут | Настроить auto-pull в Obsidian Git на 1 мин |
| **GitHub API Rate Limit** | 5000 запросов/час для аутентифицированных | Достаточно для личного использования |
| **Размер файла** | GitHub API: макс 1 MB через Contents API | Голосовые заметки редко такие большие |
| **Конфликты** | Если редактировать файл одновременно в Obsidian и через сервис | Сервис получит ошибку, нужно retry |

---

## 📁 Структура проекта

```
voice-notes-service/
├── app/
│   ├── __init__.py
│   ├── main.py                 # FastAPI приложение, точка входа
│   ├── config.py               # Конфигурация (env переменные)
│   ├── models.py               # Pydantic модели для API
│   │
│   ├── services/
│   │   ├── __init__.py
│   │   ├── transcriber.py      # OpenAI Whisper интеграция
│   │   ├── agent.py            # AI Agent (анализ и роутинг)
│   │   └── github_vault.py     # GitHub API клиент для Obsidian vault
│   │
│   └── tools/
│       ├── __init__.py
│       ├── note_tools.py       # Tools для работы с заметками
│       └── todo_tools.py       # Tools для работы с TODO
│
├── tests/
│   ├── __init__.py
│   ├── test_transcriber.py
│   ├── test_agent.py
│   └── test_github_vault.py
│
├── .env.example                # Пример переменных окружения
├── .gitignore
├── requirements.txt
├── Dockerfile
└── README.md
```

---

## 🔧 Технологический стек

| Компонент | Технология | Версия | Назначение |
|-----------|------------|--------|------------|
| **Backend** | FastAPI | 0.115+ | REST API сервер |
| **Python** | Python | 3.11+ | Язык разработки |
| **STT** | OpenAI Whisper API | whisper-1 | Транскрипция аудио |
| **AI** | OpenAI Agents SDK | 0.2+ | AI агент с tools |
| **LLM** | GPT-4o-mini | - | Модель для агента |
| **Storage** | GitHub API | v3 | Хранение заметок (Obsidian vault) |
| **Sync** | Obsidian Git plugin | - | Синхронизация vault с GitHub |
| **Hosting** | Render.com | Starter | Хостинг backend |
| **Client** | iOS Shortcuts | - | Мобильный клиент |

### Ключевые библиотеки Python

```
fastapi>=0.115.0
uvicorn>=0.30.0
openai>=1.50.0
openai-agents>=0.2.0
httpx>=0.27.0          # Для GitHub API запросов
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.9  # Для загрузки файлов
```

---

## 📡 API Specification

### POST /api/voice

Основной endpoint для приёма голосовых заметок.

**Request:**
```
Content-Type: multipart/form-data

Fields:
  - audio: binary (обязательно) — аудио файл (m4a, mp3, wav, webm)
```

**Response (200 OK):**
```json
{
  "success": true,
  "transcription": "Текст транскрипции...",
  "actions": [
    {
      "type": "note_created",
      "title": "Идея для проекта",
      "path": "Voice Notes/2025-01-17-idea.md"
    },
    {
      "type": "todo_added",
      "task": "Купить молоко",
      "file": "TODO.md"
    }
  ],
  "agent_summary": "Создана заметка 'Идея для проекта' и добавлена задача в TODO"
}
```

**Response (400 Bad Request):**
```json
{
  "success": false,
  "error": "No audio file provided"
}
```

**Response (500 Internal Server Error):**
```json
{
  "success": false,
  "error": "Transcription failed",
  "details": "..."
}
```

### GET /api/health

Health check endpoint для проверки состояния сервиса.

**Response (200 OK):**
```json
{
  "status": "healthy",
  "version": "1.0.0",
  "services": {
    "openai": "connected",
    "github": "connected"
  },
  "vault": {
    "repo": "username/obsidian-vault",
    "branch": "main"
  }
}
```

**Response (503 Service Unavailable):**
```json
{
  "status": "unhealthy",
  "version": "1.0.0",
  "services": {
    "openai": "connected",
    "github": "error: rate limit exceeded"
  }
}
```

---

## 🤖 AI Agent Specification

### Роль агента

Агент анализирует транскрипцию голосовой заметки и принимает решение о действиях.

### System Prompt

```
Ты — персональный ассистент для обработки голосовых заметок.

ТВОИ ЗАДАЧИ:
1. Проанализировать транскрипцию голосовой заметки
2. Определить тип контента и необходимые действия
3. Выполнить действия через доступные инструменты

ТИПЫ КОНТЕНТА И ДЕЙСТВИЯ:

1. ЗАДАЧИ (TODO):
   Триггеры: "нужно", "надо", "не забыть", "купить", "сделать", "позвонить", "напомни"
   Действие: add_todo_task()
   Примеры:
   - "Нужно купить молоко" → задача "Купить молоко"
   - "Не забыть позвонить маме завтра" → задача "Позвонить маме" 

2. ИДЕИ И МЫСЛИ:
   Триггеры: "идея", "мысль", "придумал", "что если", "было бы круто"
   Действие: create_note() в папку "Ideas"
   
3. РАБОЧИЕ ЗАМЕТКИ:
   Триггеры: "по работе", "проект", "задача по", "митинг", "созвон"
   Действие: create_note() в папку "Work"

4. ЛИЧНЫЕ ЗАМЕТКИ:
   Триггеры: всё остальное, дневниковые записи, размышления
   Действие: create_note() в папку "Personal"

5. СМЕШАННЫЙ КОНТЕНТ:
   Если в заметке есть И мысли И задачи — выполни ОБА действия:
   - Создай заметку с основным содержанием
   - Добавь задачи в TODO

ПРАВИЛА:
- Сохраняй смысл оригинала, но убирай слова-паразиты ("эээ", "ну", "типа")
- Форматируй заметки в Markdown
- Придумывай краткие информативные заголовки
- Задачи формулируй как действия (глагол + объект)
- Если непонятно что делать — создай заметку в Personal

ЯЗЫК: Всегда отвечай и создавай контент на русском языке.
```

### Tools (Инструменты агента)

Все tools работают через GitHub API, создавая/обновляя файлы в репозитории Obsidian vault.

#### 1. create_note

Создаёт новую заметку в Obsidian vault (через GitHub).

```python
@function_tool
async def create_note(
    title: Annotated[str, "Заголовок заметки (без расширения .md)"],
    content: Annotated[str, "Содержимое заметки в Markdown формате"],
    folder: Annotated[str, "Папка для заметки: Ideas, Work, Personal, или Voice Notes"] = "Voice Notes"
) -> str:
    """
    Создаёт новую заметку в Obsidian vault через GitHub API.
    Используй для сохранения идей, мыслей, рабочих и личных заметок.
    
    Файл будет создан по пути: {folder}/{date}-{title}.md
    """
    # Реализация через GitHub API:
    # 1. Формируем путь: folder/2025-01-17-title.md
    # 2. Добавляем frontmatter с метаданными
    # 3. Вызываем github_vault.create_file()
```

#### 2. add_todo_task

Добавляет задачу в единый TODO-файл.

```python
@function_tool
async def add_todo_task(
    task: Annotated[str, "Текст задачи (начинай с глагола)"],
    priority: Annotated[str, "Приоритет: high, medium, low"] = "medium",
    due_date: Annotated[str | None, "Дата в формате YYYY-MM-DD или None"] = None
) -> str:
    """
    Добавляет новую задачу в файл TODO.md в Obsidian vault.
    Используй для всего, что нужно сделать, купить, не забыть.
    
    Задача добавляется в соответствующую секцию по приоритету.
    """
    # Реализация через GitHub API:
    # 1. Получаем текущий TODO.md через github_vault.get_file()
    # 2. Парсим секции по приоритетам
    # 3. Добавляем задачу в нужную секцию
    # 4. Обновляем файл через github_vault.update_file()
```

#### 3. append_to_note

Дополняет существующую заметку.

```python
@function_tool
async def append_to_note(
    note_path: Annotated[str, "Путь к заметке относительно vault (например: Work/Project X.md)"],
    content: Annotated[str, "Контент для добавления в Markdown"]
) -> str:
    """
    Добавляет контент в конец существующей заметки.
    Используй когда пользователь явно говорит "добавь к заметке X" или "дополни".
    """
    # Реализация через GitHub API:
    # 1. Получаем текущий файл через github_vault.get_file()
    # 2. Добавляем новый контент в конец
    # 3. Обновляем файл через github_vault.update_file()
```

#### 4. list_notes (вспомогательный)

Получает список заметок в папке (для поиска существующих).

```python
@function_tool
async def list_notes(
    folder: Annotated[str, "Папка для поиска: Ideas, Work, Personal, Voice Notes"] = "Voice Notes",
    search_query: Annotated[str | None, "Поиск по названию (опционально)"] = None
) -> str:
    """
    Возвращает список заметок в указанной папке.
    Используй чтобы найти существующую заметку перед append_to_note.
    """
    # Реализация через GitHub API:
    # 1. Получаем содержимое папки через github_vault.list_folder()
    # 2. Фильтруем по search_query если указан
    # 3. Возвращаем список файлов
```

---

## 📝 Obsidian Structure

### Структура папок в Vault

```
Obsidian Vault/
├── TODO.md                     # Единый файл задач
├── Voice Notes/                # Все голосовые заметки (по умолчанию)
│   └── 2025-01-17-meeting.md
├── Ideas/                      # Идеи и мысли
│   └── 2025-01-17-app-idea.md
├── Work/                       # Рабочие заметки
│   └── Project-X.md
└── Personal/                   # Личные заметки
    └── 2025-01-17-reflection.md
```

### Формат TODO.md

```markdown
# 📋 TODO

## 🔴 High Priority
- [ ] Задача с высоким приоритетом

## 🟡 Medium Priority  
- [ ] Купить молоко
- [ ] Позвонить маме

## 🟢 Low Priority
- [ ] Задача с низким приоритетом

## ✅ Completed
- [x] Завершённая задача (2025-01-15)
```

### Формат заметки из голосового сообщения

```markdown
---
created: 2025-01-17T14:30:00
source: voice
tags: [voice-note, idea]
---

# Идея для нового приложения

Содержимое заметки, отформатированное в Markdown...

---
*Создано из голосовой заметки*
```

---

## 🔌 GitHub Vault Service Specification

Сервис для работы с Obsidian vault через GitHub API.

### Класс GitHubVaultService

```python
# app/services/github_vault.py

from dataclasses import dataclass
from datetime import datetime
import httpx
import base64

@dataclass
class FileInfo:
    """Информация о файле в репозитории"""
    path: str
    sha: str
    content: str | None = None

class GitHubVaultService:
    """
    Сервис для работы с Obsidian vault через GitHub API.
    
    Все операции с файлами проходят через GitHub REST API v3.
    Для обновления файлов требуется SHA текущей версии (оптимистичная блокировка).
    """
    
    def __init__(
        self,
        token: str,
        repo_owner: str,
        repo_name: str,
        branch: str = "main"
    ):
        self.token = token
        self.repo_owner = repo_owner
        self.repo_name = repo_name
        self.branch = branch
        self.base_url = f"https://api.github.com/repos/{repo_owner}/{repo_name}"
        
    async def get_file(self, path: str) -> FileInfo | None:
        """
        Получает содержимое файла из репозитория.
        
        Args:
            path: Путь к файлу относительно корня vault (например: "TODO.md")
            
        Returns:
            FileInfo с содержимым и SHA, или None если файл не найден
        """
        
    async def create_file(
        self,
        path: str,
        content: str,
        commit_message: str
    ) -> FileInfo:
        """
        Создаёт новый файл в репозитории.
        
        Args:
            path: Путь для нового файла
            content: Содержимое файла (текст)
            commit_message: Сообщение коммита
            
        Returns:
            FileInfo созданного файла
            
        Raises:
            FileExistsError: Если файл уже существует
        """
        
    async def update_file(
        self,
        path: str,
        content: str,
        sha: str,
        commit_message: str
    ) -> FileInfo:
        """
        Обновляет существующий файл.
        
        Args:
            path: Путь к файлу
            content: Новое содержимое
            sha: SHA текущей версии (для оптимистичной блокировки)
            commit_message: Сообщение коммита
            
        Returns:
            FileInfo обновлённого файла
            
        Raises:
            ConflictError: Если SHA не совпадает (файл был изменён)
        """
        
    async def list_folder(self, folder_path: str) -> list[str]:
        """
        Получает список файлов в папке.
        
        Args:
            folder_path: Путь к папке (например: "Voice Notes")
            
        Returns:
            Список путей к файлам в папке
        """
        
    async def create_or_update_file(
        self,
        path: str,
        content: str,
        commit_message: str
    ) -> FileInfo:
        """
        Создаёт файл или обновляет если существует.
        
        Удобный метод, который сам определяет нужно ли создавать или обновлять.
        """
        existing = await self.get_file(path)
        if existing:
            return await self.update_file(path, content, existing.sha, commit_message)
        else:
            return await self.create_file(path, content, commit_message)
```

### Пример использования

```python
# В tools
vault = GitHubVaultService(
    token=settings.github_token,
    repo_owner=settings.github_repo_owner,
    repo_name=settings.github_repo_name
)

# Создание заметки
await vault.create_file(
    path="Ideas/2025-01-17-cool-idea.md",
    content="# Крутая идея\n\nОписание...",
    commit_message="Voice note: Крутая идея"
)

# Обновление TODO
todo = await vault.get_file("TODO.md")
new_content = add_task_to_todo(todo.content, "Купить молоко", "medium")
await vault.update_file(
    path="TODO.md",
    content=new_content,
    sha=todo.sha,
    commit_message="Add task: Купить молоко"
)
```

---

## ⚙️ Конфигурация

### Переменные окружения (.env)

```bash
# OpenAI
OPENAI_API_KEY=sk-...

# GitHub (для доступа к Obsidian vault)
GITHUB_TOKEN=ghp_...                    # Personal Access Token с правами repo
GITHUB_REPO_OWNER=your-username         # Владелец репозитория
GITHUB_REPO_NAME=obsidian-vault         # Название репозитория
GITHUB_BRANCH=main                      # Ветка (обычно main)

# App Settings
APP_ENV=development
LOG_LEVEL=INFO

# Optional: для Render.com
PORT=8000
```

### Получение GitHub Token

1. Перейди в GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Нажми "Generate new token (classic)"
3. Выбери scopes:
   - `repo` (полный доступ к приватным репозиториям)
4. Скопируй токен (он показывается только один раз!)

### Настройка репозитория для Obsidian vault

**Если vault ещё не в GitHub:**

```bash
cd /path/to/your/obsidian/vault
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/username/obsidian-vault.git
git push -u origin main
```

**Рекомендации:**
- Сделай репозиторий **приватным** (личные заметки!)
- Добавь `.obsidian/workspace.json` в `.gitignore` (меняется постоянно)

### Настройка Obsidian Git плагина

1. В Obsidian: Settings → Community plugins → Browse
2. Найди и установи "Obsidian Git"
3. Настройки плагина:
   - **Auto pull interval**: 5 минут
   - **Auto push interval**: 10 минут (или отключить, если пушишь вручную)
   - **Pull on startup**: включить
   - **Disable notifications**: по желанию

Теперь заметки, созданные сервисом, появятся в Obsidian через 5 минут автоматически.

---

## 📱 iOS Shortcuts Setup

### Shortcut: "Voice Note"

**Блоки:**

1. **Record Audio**
   - Stop recording: After 60 seconds (или On Tap)
   - Audio Quality: Normal

2. **Get Contents of URL**
   - URL: `https://your-app.onrender.com/api/voice`
   - Method: POST
   - Headers:
     - Content-Type: multipart/form-data
   - Request Body: Form
     - audio: [Recorded Audio]

3. **Get Dictionary Value**
   - Get value for: `agent_summary`
   - in: [Response from step 2]

4. **Show Notification**
   - Title: "✅ Заметка обработана"
   - Body: [Value from step 3]

### Альтернативный Shortcut: "Quick Task"

Упрощённая версия только для задач:

1. **Dictate Text** (диктовка вместо записи)
2. **Get Contents of URL** (отправка текста напрямую)
3. **Show Notification**

---

## 🧪 Тестирование

### Unit Tests

```python
# tests/test_github_vault.py

import pytest
from app.services.github_vault import GitHubVaultService

@pytest.mark.asyncio
async def test_create_file(mock_github_api):
    """Тест: создание файла в репозитории"""
    vault = GitHubVaultService(token="test", repo_owner="user", repo_name="vault")
    result = await vault.create_file(
        path="test.md",
        content="# Test",
        commit_message="test commit"
    )
    assert result.path == "test.md"
    assert result.sha is not None

@pytest.mark.asyncio
async def test_update_todo(mock_github_api):
    """Тест: обновление TODO.md"""
    vault = GitHubVaultService(token="test", repo_owner="user", repo_name="vault")
    # Сначала получаем файл
    todo = await vault.get_file("TODO.md")
    # Обновляем
    result = await vault.update_file(
        path="TODO.md",
        content=todo.content + "\n- [ ] New task",
        sha=todo.sha,
        commit_message="Add task"
    )
    assert "New task" in result.content
```

```python
# tests/test_agent.py

import pytest
from app.services.agent import process_transcription

@pytest.mark.asyncio
async def test_todo_detection():
    """Тест: агент распознаёт задачи"""
    result = await process_transcription("Нужно купить молоко и хлеб")
    assert any(a["type"] == "todo_added" for a in result["actions"])

@pytest.mark.asyncio  
async def test_idea_note_creation():
    """Тест: агент создаёт заметку для идей"""
    result = await process_transcription("У меня есть идея для приложения...")
    assert any(a["type"] == "note_created" for a in result["actions"])
    assert any("Ideas" in a.get("path", "") for a in result["actions"])

@pytest.mark.asyncio
async def test_mixed_content():
    """Тест: агент обрабатывает смешанный контент"""
    result = await process_transcription(
        "Классная идея для проекта - сделать голосовые заметки. "
        "Нужно не забыть купить микрофон."
    )
    actions = result["actions"]
    assert any(a["type"] == "note_created" for a in actions)
    assert any(a["type"] == "todo_added" for a in actions)
```

### Integration Tests

```python
# tests/test_integration.py

import pytest
from fastapi.testclient import TestClient
from app.main import app

@pytest.mark.asyncio
async def test_voice_endpoint_e2e(test_audio_file):
    """End-to-end тест: загрузка аудио → транскрипция → действия"""
    client = TestClient(app)
    
    with open(test_audio_file, "rb") as f:
        response = client.post(
            "/api/voice",
            files={"audio": ("test.m4a", f, "audio/mp4")}
        )
    
    assert response.status_code == 200
    data = response.json()
    assert data["success"] is True
    assert "transcription" in data
    assert "actions" in data
```

### Manual Testing Checklist

- [ ] Аудио файл успешно загружается (m4a, mp3, wav)
- [ ] Транскрипция работает на русском языке
- [ ] Агент корректно определяет тип контента
- [ ] Заметки создаются в правильных папках в GitHub
- [ ] TODO.md обновляется корректно (правильная секция приоритета)
- [ ] Obsidian Git подтягивает изменения
- [ ] Заметки появляются в Obsidian
- [ ] iOS Shortcut работает end-to-end
- [ ] Ошибки обрабатываются gracefully (нет 500 ошибок)

---

## 🚀 Deployment

### Render.com Setup

1. **Создать Web Service**
   - Repository: GitHub repo с кодом сервиса
   - Branch: main
   - Runtime: Python 3
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`

2. **Environment Variables**
   - `OPENAI_API_KEY` — ключ OpenAI
   - `GITHUB_TOKEN` — токен GitHub
   - `GITHUB_REPO_OWNER` — владелец репо с vault
   - `GITHUB_REPO_NAME` — название репо с vault
   - `GITHUB_BRANCH` — ветка (main)

3. **Health Check**
   - Path: `/api/health`

### Checklist перед деплоем

- [ ] Obsidian vault в приватном GitHub репозитории
- [ ] GitHub Token создан с правами `repo`
- [ ] Obsidian Git плагин настроен на auto-pull
- [ ] Все env переменные добавлены в Render
- [ ] Структура папок в vault создана (Ideas, Work, Personal, Voice Notes)
- [ ] TODO.md создан с начальной структурой

---

## 📅 План разработки

### Фаза 1: Core Backend (День 1-2)
- [ ] Инициализация проекта (FastAPI, структура папок)
- [ ] Конфигурация и переменные окружения (pydantic-settings)
- [ ] Endpoint POST /api/voice (заглушка)
- [ ] Endpoint GET /api/health
- [ ] Базовые Pydantic модели

### Фаза 2: Transcription (День 2-3)
- [ ] Интеграция OpenAI Whisper API
- [ ] Обработка разных форматов аудио (m4a, mp3, wav)
- [ ] Сохранение временных файлов
- [ ] Обработка ошибок транскрипции
- [ ] Тесты для transcriber

### Фаза 3: GitHub Vault Integration (День 3-4)
- [ ] GitHub API клиент (github_vault.py)
- [ ] Метод create_file() — создание заметок
- [ ] Метод update_file() — обновление файлов
- [ ] Метод get_file() — чтение файлов
- [ ] Метод list_folder() — список файлов в папке
- [ ] Обработка конфликтов (SHA для обновлений)
- [ ] Тесты для GitHub клиента

### Фаза 4: AI Agent (День 4-5)
- [ ] Настройка OpenAI Agents SDK
- [ ] System prompt для агента
- [ ] Tool: create_note
- [ ] Tool: add_todo_task
- [ ] Tool: append_to_note
- [ ] Tool: list_notes
- [ ] Интеграция tools с github_vault
- [ ] Тесты для агента

### Фаза 5: Integration & Testing (День 6)
- [ ] Интеграция всех компонентов в main.py
- [ ] End-to-end тестирование пайплайна
- [ ] Обработка edge cases
- [ ] Логирование
- [ ] iOS Shortcut создание и тест

### Фаза 6: Deployment (День 7)
- [ ] Подготовка Obsidian vault в GitHub
- [ ] Настройка Obsidian Git плагина
- [ ] Создание структуры папок и TODO.md
- [ ] Деплой на Render.com
- [ ] Тестирование в production
- [ ] README и документация

---

## 🔮 Будущие улучшения (v2.0)

- [ ] Telegram Bot как альтернативный клиент
- [ ] Распознавание спикеров (если несколько человек)
- [ ] Автоматические теги на основе контента
- [ ] Поиск по существующим заметкам перед созданием новых
- [ ] Напоминания о задачах
- [ ] Аналитика использования
- [ ] Offline режим с синхронизацией

---

## 📚 Ресурсы

- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [OpenAI Whisper API](https://platform.openai.com/docs/guides/speech-to-text)
- [OpenAI Agents SDK](https://github.com/openai/openai-agents-python)
- [GitHub REST API - Contents](https://docs.github.com/en/rest/repos/contents) — работа с файлами
- [Obsidian Git Plugin](https://github.com/denolehov/obsidian-git)
- [iOS Shortcuts Guide](https://support.apple.com/guide/shortcuts/welcome/ios)
- [httpx Documentation](https://www.python-httpx.org/) — HTTP клиент для Python